# Quarto Report Template

This is a minimal starter template for a Quarto report.

Copy the folder, rename `template.qmd`, then render:

```bash
quarto render template.qmd --to html
quarto render template.qmd --to pdf
```

